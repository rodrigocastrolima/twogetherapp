rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Very simple rules for initial testing - allow all authenticated users access to all collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 